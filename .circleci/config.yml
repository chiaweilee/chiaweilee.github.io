version: 2.0
orbs:
  node: circleci/node@1.1.6
jobs:
  build:
    branches:
      only: master
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install --legacy-peer-deps
      - run: sh deploy-circleci.sh
